// client side -> javascript, html and css
// server side -> nodejs which connects with database
// client gives request and server fulfills the request in form of response in form of APIs
// chrome has a v8 engine which is similar to node and which complies js

// servers is connected with client with nodejs as it makes apis for connections
// nodejs is a runtime environment

const name = "Manya";
console.log(name);
